# Core application dependencies
flask
requests
onnxruntime
scikit-learn
skl2onnx
numpy

# Linting and type checking dependencies
flake8
mypy
types-requests

# Phase 2: LangChain and gRPC dependencies
langchain>=0.1.0
langchain-community>=0.0.10
grpcio>=1.60.0
grpcio-tools>=1.60.0
joblib

# Phase 3: Advanced caching dependencies
Flask-Caching>=2.0.0
redis>=4.0.0
fastembed>=0.2.0

# Phase 4: Production model lifecycle & monitoring dependencies
evidently>=0.4.0
mlflow>=2.9.0
pandas>=1.5.0

# Phase 5: Dedicated model serving infrastructure dependencies
# NOTE: Using compatible TensorFlow version with Phase 6 dependencies
tensorflow==2.16.1
tf2onnx>=1.14.0
tensorflow-serving-api==2.16.1
tritonclient[http]>=2.37.0

# Phase 6: Advanced RAG (Retrieval-Augmented Generation) dependencies
# Using specific compatible versions to avoid protobuf conflicts
llama-index==0.13.0
llama-index-llms-ollama==0.7.0
llama-index-embeddings-huggingface==0.6.0
llama-index-vector-stores-chroma==0.5.0
chromadb==1.0.15
pypdf==5.9.0
python-docx==1.2.0

# Explicit protobuf version compatible with both TensorFlow and LlamaIndex
protobuf>=4.25.0,<5.0.0